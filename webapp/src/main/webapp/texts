
import edu.harvard.chs.cite.CtsUrn
boolean done = false

// evaluate params.  Is this a reasonable CTS request?

if (!done) {
  response.setContentType("text/xml")
  response.setCharacterEncoding('UTF-8')
  response.setHeader( "Access-Control-Allow-Origin", "*")

  // determine reply for request and print it
  
  println "<html><head><title>CTS!</title></head><body><p>Insert body.</p><p>Note doc type is pure text/xml</p></body></html>\n"
}
